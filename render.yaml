services:
  - type: web
    name: neogents-tech
    runtime: node
    buildCommand: cd artifacts/mockup-sandbox && npm ci && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
